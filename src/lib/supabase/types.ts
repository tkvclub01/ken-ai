/**
 * ⚠️ DEPRECATED - This file is kept for backward compatibility only.
 * 
 * All types are now centralized in @/types/index.ts
 * To regenerate types from Supabase, run: npm run db:types
 * 
 * Import from @/types instead:
 *   import { Database, Student, Document } from '@/types'
 */

// Re-export everything from the canonical types file
export * from '@/types'
